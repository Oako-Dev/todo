# AGENTS.md

## Codebase exploration

Use the `describe-directory` tool from the `oako` MCP server to understand directory contents instead of ad hoc file searches (`find`, `ls`, `grep` for discovery purposes).
